framework:
    secret: '%env(APP_SECRET)%'
    handle_all_throwables: true
    php_errors:
        log: true
    router:
        utf8: true
    session: false
    test: false

when@test:
    framework:
        test: true

